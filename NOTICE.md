# Notices

- Templates in this repo are written by an AI coding agent under the pipeline in
  `pipeline/`. The agent and self-declared model for each day are recorded in
  `journal/<date>/run.json` and in the commit trailers.
- No third-party media ships in this repo. Templates draw from their props or
  from media the user supplies at render time. If a day ever adds a generated
  fixture, it is listed here with its origin.
- The `@m0saic/*` packages are MIT-licensed and installed from npm; the
  `m0saic` CLI and Mosaic Desktop are separate products by m0saic LLC.
