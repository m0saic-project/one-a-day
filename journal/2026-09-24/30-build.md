# Build - 2026-09-24

## Template: @one-a-day/dev/app-store-screenshot-frame/v1

## Variant a - centered stack - gate: clean (one unrelated pre-existing og-card warning) - render: ok - stills: portrait stacks the copy over a dominant generic phone; square keeps the same hierarchy in a compact stage; landscape switches to a left copy rail and right device stage.

## Why-tutorial: the problem page says one capture multiplies across screens, devices, locales, and release updates, citing the reported three-hour release, 60-file workflow, and Fastlane's 600-capture example. The solution page says the template makes one deterministic PNG and preserves the complete capture with contain placement inside a generic 9:19.5 shell.

## What was hard

The earlier source rollback left an empty template directory while the successful variant snapshot survived. Recovery required recreating registry wiring, restoring the snapshot, correcting the explicit runner model, and regenerating the fingerprint and tutorial without redesigning the template.

The scaffold's placeholder registry description survived the first clean gate, so it needed a manual metadata pass before the final build. The token-cost audit remains preserved for the supervising session and ship journal; its verified GPT-5.6 Sol estimate excludes unreported failed turns and supervising-chat usage.

## In place now: a
